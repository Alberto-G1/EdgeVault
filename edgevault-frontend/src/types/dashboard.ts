export interface StatCard {
    title: string;
    value: number;
    change: string;
}

export interface RecentActivity {
    username: string;
    action: string;
    details: string;
    timestamp: string;
}